# YeMu AI Novel 功能完善 TODO

> 对标范围：作品管理与写作页面。
> 更新日期：2026-07-23。
> 当前判断：本轮功能闭环、移动端适配和服务端上线验证已完成；真实浏览器点击验收待补。

## 已完成

- [x] 写作页 AI 工具：续写、扩写、润色、纠错、灵感风暴、AI 编辑和工作流入口。
- [x] 写作页侧栏：章节目录、全书大纲、人物、词条，以及右侧 AI 助手收起/展开。
- [x] 正文编辑工具：撤销、重做、加粗、斜体、复制、全选、搜索、素材插入和自动排版。
- [x] 编辑辅助：按输入批次记录历史、高频词分析与定位、智能分章及失败回滚。
- [x] AI 结果回写：复制、插入光标、替换选区、替换全文，并接入撤销历史。
- [x] 章节管理：正序/倒序、只看未完成、完成状态切换、从全书大纲跳转章节。
- [x] 内容输出：导出本章、导出全书、朗读和阅读模式。
- [x] 作品管理：类型/状态筛选、智能创建入口、本地 TXT 导入与章节标题识别预览。
- [x] 后端导入接口：章节数、单章字数、单次总字数限制及 API 冒烟测试覆盖。

## P0：功能闭环（3 项）

- [x] **智能创建作品闭环**：将 AI 返回的书名、题材、主线和章节大纲直接生成作品与初始章节，成功后进入写作页；补齐重复提交、失败恢复和结果校验。
- [x] **删除作品二次确认**：展示作品名和不可恢复提示；删除期间禁用重复操作，失败时保留原数据并提示原因。
- [x] **编辑历史持久化**：当前历史只在本次页面会话内有效；需要按作品和章节保存快照，刷新后仍可查看并恢复。

## P1：体验优化（2 项）

- [x] **窄屏与移动端适配**：检查写作页三栏、工具栏溢出、弹窗、章节菜单和触屏操作。
- [x] **完整状态反馈**：统一智能创建、导入、导出、保存、分章、删除等操作的加载、成功、空状态和错误提示。

## P2：验证与上线（3 项）

- [x] **重启本地 API 服务**：让新增的 `POST /api/projects/import` 接口在当前运行进程中生效。
- [x] **完整自动验证**：运行 `npm run build`、`npm test` 和 `git diff --check`，修复所有新增回归。
- [ ] **真实浏览器点击验收**：登录本地站后，对照参考站逐项检查作品管理、写作工具、章节切换、AI 回写、导入导出和刷新恢复（当前环境未安装 Chromium/Firefox 及 Playwright/Puppeteer，已完成页面生产构建和 API/AI 集成验收）。
- [x] **统一创作入口**：总览页全局助手持久化收集篇幅、题材、流派和故事设定，生成方案后复用原子建书接口；编辑器助手按作品篇幅路由长篇/短篇 Skill。
- [x] 章节上下文编排：AI 请求自动携带作品设定、当前章节大纲、最近章节结尾、关联素材和未回收伏笔。
- [x] 局部重写与结果对比：选区支持相近长度、扩写和精简，结果确认后才替换正文。
- [x] 可恢复 AI 任务：任务状态、进度、结果、错误和取消操作持久化，服务重启会标记中断任务。
- [x] 伏笔生命周期：支持计划中、已埋入、已回收、已放弃，以及埋入/计划回收/实际回收章节。
- [x] PostgreSQL 过渡存储与 Docker Compose：以 JSONB 单行保持 API 兼容，提供导入、导出和状态检查命令。

## 本轮验收标准

- 智能创建、普通新建和 TXT 导入都能生成可继续编辑的作品。
- 写作页所有可见按钮均有真实行为，不出现仅展示但无法操作的入口。
- AI 生成内容可以安全写回正文，并可撤销、重做和恢复历史版本。
- 章节状态、排序、分章、导入、导出在刷新后数据保持一致。
- 构建、自动测试和浏览器主流程全部通过。

## 后续数据库演进

- [ ] 将 `users`、`sessions`、`projects`、`chapters`、`drafts` 和 `ideas` 从 JSONB 状态逐表拆为关系表，保留现有 HTTP API。
- [x] 用 Redis 持久化夜雨写作助手聊天记忆（AOF），不可用时回退 JSON/Postgres。
- [ ] 将 AI 任务执行从 Node 进程迁移到 Redis 队列和独立 worker，支持多副本扩展。
- [ ] 增加 PostgreSQL Compose 集成测试和备份恢复演练。
