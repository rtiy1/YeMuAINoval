提交本次 Story Agent 的结构化结果。普通回答使用 output，正文修改使用 edit_proposal，作品结构变更使用 artifacts，审稿可附 findings。如果人物关系发生变化，在 artifacts.relationship_graph 中提供 nodes（角色）和 edges（关系）数据。已经通过 write_story_file / edit_story_file 写入的文件无需再次放进 artifacts，只需在 output 或 summary 中简要说明。提交后当前执行结束。